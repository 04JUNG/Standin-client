{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "기본 창 권한 + 소셜 로그인용 외부 열기(opener)·딥링크 수신(deep-link). 최소 capability 원칙(docs/11 §3).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-fullscreen",
    "opener:default",
    "deep-link:default"
  ]
}
