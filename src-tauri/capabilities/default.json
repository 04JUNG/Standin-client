{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "기본 창 권한 + 소셜 로그인용 외부 열기(opener)·딥링크 수신(deep-link). 창 조작·전역 단축키는 자체 command가 담당하므로 권한이 필요 없다. 최소 capability 원칙(docs/11 §3).",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default", "deep-link:default"]
}
