# Standin Desktop App — Claude 개발 지침

## 1. 프로젝트 정의

Standin Desktop은 웹툰 작가가 러프 콘티 또는 화면 캡처를 입력하면, 서버 분석을 통해 가까운 3D 포즈 후보를 받고, 후보를 비교·선택한 뒤 포즈 파일로 저장하는 **Tauri 기반 PC 앱**이다.

제품의 핵심 흐름:

```text
로그인
→ 파일 업로드 또는 화면 캡처
→ 분석 요청
→ VLM·스켈레톤·포즈 검색 진행 상태 확인
→ 포즈 후보 5개 비교
→ 후보 선택 또는 다시 검색
→ 포즈 파일 저장
```

이 저장소는 **Tauri 데스크톱 앱만 담당**한다.

- Python FastAPI 서버 구현은 별도 저장소에 있다.
- Next.js 랜딩페이지도 별도 저장소에 있다.
- 이 저장소에서는 서버 API를 호출하는 클라이언트와 데스크톱 네이티브 기능을 구현한다.

---

## 2. 이번 주 우선순위

이번 주의 사용자 담당 범위는 아래 순서를 따른다.

1. 전체 문서와 레포 구조 정립
2. 앱 UI 기반 구현
3. 로그인·로그아웃 기본 기능
4. 화면 캡처 기능
5. 캡처 결과를 업로드 입력으로 연결하는 준비

이번 주에는 아래 기능을 완성하려고 범위를 넓히지 않는다.

- 실제 VLM 및 포즈 검색 알고리즘
- 실제 BVH 생성
- 고급 3D 관절 편집기
- 화면 녹화의 완성형 구현
- 자동 업데이트 및 결제
- 클립스튜디오 자동 실행
- 다중 모니터의 모든 예외 처리

---

## 3. 반드시 읽을 문서

구현 전 아래 문서를 순서대로 읽는다.

1. `docs/01_PRODUCT_AND_MVP.md`
2. `docs/02_APP_ARCHITECTURE.md`
3. `docs/03_USER_FLOW_AND_SCREENS.md`
4. `docs/04_DESKTOP_UI_GUIDE.md`
5. `docs/05_REPOSITORY_AND_COLLABORATION.md`
6. `docs/06_AUTH_SPEC.md`
7. `docs/07_CAPTURE_AND_UPLOAD_SPEC.md`
8. `docs/08_API_CONTRACT.md`
9. `docs/09_STATE_AND_DATA_MODEL.md`
10. `docs/10_THIS_WEEK_PLAN.md`
11. `docs/11_QA_SECURITY_RELEASE.md`

문서가 충돌하면 아래 우선순위를 따른다.

```text
CLAUDE.md
→ 01_PRODUCT_AND_MVP.md
→ 기능별 명세
→ UI 가이드
→ 구현 중 작성된 ADR
```

---

## 4. 기술 방향

### 기본 스택

- Tauri
- React
- TypeScript
- Vite
- Rust: 네이티브 기능과 Tauri command
- TanStack Query: 서버 상태
- Zustand: 앱 전역 UI 상태
- React Hook Form + Zod: 폼과 입력 검증
- Tailwind CSS 또는 CSS Modules 중 프로젝트에서 확정한 하나
- Lucide React: 아이콘
- Three.js / react-three-fiber: 실제 3D 후보 뷰어가 필요할 때만 도입

패키지는 구현 시점의 안정 버전을 확인하고 lockfile에 고정한다. 기억에 의존해 버전 번호를 문서에 하드코딩하지 않는다.

---

## 5. 핵심 설계 원칙

### 빠른 입력

사용자는 앱을 열고 가능한 적은 동작으로 파일을 넣거나 화면을 캡처할 수 있어야 한다.

### 명확한 상태

서버 분석이 오래 걸릴 수 있으므로 현재 단계, 실패 여부, 재시도 가능 여부를 명확히 보여준다.

### 작가 선택 중심

포즈 후보 중 하나를 앱이 자동 확정하지 않는다. 사용자가 5개 후보를 비교하고 직접 선택한다.

### 비파괴적 흐름

원본 파일을 덮어쓰지 않는다. 캡처, 분석 결과, 선택 결과를 별도 작업 항목으로 관리한다.

### 서버와 앱의 분리

앱은 모델 추론 로직을 포함하지 않는다. 서버 API를 통해 분석을 요청하고 결과를 표시한다.

### 네이티브 기능 최소화

파일 시스템, 전역 단축키, 화면 캡처, 저장 대화상자처럼 데스크톱 권한이 필요한 기능만 Rust/Tauri 계층에 둔다. 일반 UI와 비즈니스 로직은 TypeScript에 둔다.

---

## 6. 제품 표현 규칙

사용자에게 다음을 약속하지 않는다.

- 모든 콘티를 정확히 분석
- 완벽한 포즈 하나를 자동 생성
- 모든 다중 인물 장면 지원
- 모든 컷의 자동 3D 배치
- 클립스튜디오 공식 플러그인
- 서버 처리 시간이 항상 짧음

다음 표현을 우선한다.

- 가까운 포즈 후보
- 분석 중
- 다시 검색
- 직접 선택
- 보정이 필요할 수 있음
- 현재 지원 범위
- 저장할 포즈 선택

---

## 7. 기본 라우트

```text
/auth/login
/app/home
/app/jobs/:jobId
/app/settings
```

초기 버전에서 별도 라우터가 과하면 상태 기반 전환도 가능하지만, 인증 화면과 앱 화면은 분리한다.

---

## 8. 기본 기능 단위

```text
features/
├─ auth
├─ capture
├─ upload
├─ analysis
├─ pose-viewer
├─ export
└─ settings
```

기능 간 직접 import를 남발하지 않는다. 공통 타입과 UI는 `shared/`에서 관리한다.

---

## 9. 네이티브 경계

Tauri command가 담당할 수 있는 작업:

- 화면 캡처 시작 및 결과 경로 반환
- 파일 선택 대화상자
- 저장 대화상자
- 앱 전역 단축키 등록
- 앱 데이터 디렉터리 접근
- 운영체제별 권한 확인
- 임시 파일 정리

Tauri command가 담당하지 않을 작업:

- 로그인 폼 검증
- 후보 카드 선택
- API 응답 가공
- 토스트 상태
- 페이지 레이아웃
- 일반적인 HTTP 요청

---

## 10. 코드 작성 원칙

- TypeScript `any` 사용을 피한다.
- API 응답은 Zod 또는 명시적 타입으로 검증한다.
- 한 컴포넌트가 화면 전체의 모든 로직을 소유하지 않게 한다.
- Rust command는 작고 단일 책임으로 작성한다.
- 운영체제 분기는 한 파일에 모은다.
- 실패 가능한 네이티브 작업은 사용자에게 복구 가능한 오류를 반환한다.
- 비밀정보와 토큰을 로그에 출력하지 않는다.
- 하드코딩된 서버 URL을 사용하지 않는다.
- 임시 Mock과 실제 API 모드를 구분한다.
- 구현하지 않은 기능을 작동하는 것처럼 보이게 하지 않는다.

---

## 11. UI 품질 기준

- 랜딩페이지와 동일한 Brand Ink, Coral, Sky, Paper 컬러 사용
- 앱은 더 조밀한 8px 기반 간격 체계 사용
- 좌측 사이드바 또는 상단 앱 바 중 하나로 일관
- 네이티브 앱처럼 창 크기가 줄어들어도 핵심 동작 유지
- 최소 권장 창 크기: 960×640
- 기본 권장 창 크기: 1280×800
- 캡처·업로드 버튼은 홈 화면에서 가장 강하게 표시
- 처리 중인 작업은 앱을 닫거나 이동하기 전에 상태를 알 수 있어야 함
- 모든 클릭 요소에 hover, focus, disabled, loading 상태 구현

---

## 12. 작업 완료 보고 형식

작업 종료 시 다음을 정리한다.

1. 변경한 파일
2. 구현한 사용자 흐름
3. 사용한 Mock 또는 실제 API
4. Windows/macOS에서 확인한 범위
5. 알려진 한계
6. 다음 작업자가 이어서 할 TODO

---

## 13. 완료 조건

### 이번 주

- 앱이 실행됨
- 로그인 화면과 인증 상태 전환이 동작함
- 로그아웃이 동작함
- 홈 화면이 UI 가이드에 맞게 구현됨
- 파일 선택 UI가 동작함
- 캡처 버튼으로 캡처 흐름에 진입 가능함
- 캡처 결과를 미리 보고 취소 또는 사용 가능함
- 캡처 결과가 업로드 대기 항목으로 들어감
- 에러와 권한 거부 상태가 표시됨
- README에 실행법과 환경변수가 정리됨
- 협업용 브랜치 및 PR 규칙이 문서화됨

### MVP 전체

- 로그인·로그아웃
- 파일 업로드
- 캡처 입력
- 서버 분석 요청과 진행 상태
- 포즈 후보 5개 뷰어
- 후보 선택 및 다시 검색
- 포즈 결과 저장
