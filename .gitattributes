# 모든 텍스트 파일을 저장소에서 LF로 정규화(체크아웃도 LF). Windows CRLF 경고 제거.
* text=auto eol=lf

# 바이너리 자산은 정규화하지 않음.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.icns binary
*.woff binary
*.woff2 binary
